/*
Given a non-empty array of integers, return the result of multiplying the values together in order. Example:

[1, 2, 3, 4] => 1 * 2 * 3 * 4 = 24
*/

// ans 

    int grow(List<int> arr) {
 int result = 0 ;
if(arr.isEmpty){
  return 0;
}else{
  result = arr.reduce((value ,element) => value * element);
  }
  return result ;
}
  
